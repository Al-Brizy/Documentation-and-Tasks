# Second Real Python Project

name = "Bobby"
#name = "" # case if user doesn't put their name
age = 31
gpa = 3.7
is_student = True

age += 1 # for int()/float()

name = bool(name) # for checking user if they put their name or not
age = str(age)
gpa = int(gpa)

age += "1" # for str()

print(age)
print(gpa)
print(name)